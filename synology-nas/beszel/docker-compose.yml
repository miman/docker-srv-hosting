# docker-compose.yml for Beszel

version: "3.9"
# Specify the Docker Compose file format version.
# Version 3.9 is a good modern choice, compatible with most Docker installations.

services:
   beszel:
      image: henrygd/beszel:latest
      container_name: beszel
      restart: unless-stopped
      environment:
         APP_URL: http://localhost:4533
      ports:
         - 4533:8090
      volumes:
         - /volume1/docker/beszel/data:/beszel_data
         - /volume1/docker/beszel/socket:/beszel_socket
      # healthcheck:
      #   test: ['CMD', '/beszel', 'health', '--url', 'http://localhost:8090']
      #   interval: 120s
      #   start_period: 10s
      #   timeout: 5s

   beszel-agent:
      image: henrygd/beszel-agent:latest
      container_name: beszel-agent
      restart: unless-stopped
      network_mode: host
      volumes:
         - /volume1/docker/beszel/agent_data:/var/lib/beszel-agent
         - /volume1/docker/beszel/socket:/beszel_socket
         - /var/run/docker.sock:/var/run/docker.sock:ro
      environment:
         LISTEN: /beszel_socket/beszel.sock
         HUB_URL: http://localhost:4533
         TOKEN: <token>
         KEY: "<key>"
      # healthcheck:
      #   test: ['CMD', '/agent', 'health']
      #   interval: 120s

